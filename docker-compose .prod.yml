services:
  web:
    build:
      context: .
      dockerfile: docker/prod/Dockerfile

    container_name: compumercado-web

    ports:
      - "3000:3000"

    environment:
      NODE_ENV: production

    restart: unless-stopped